{
  "name": "bachelors-project-app",
  "version": "0.1.0",
  "main": "public/main.js",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "classnames": "^2.2.6",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron-start": "electron .",
    "preelectron-pack": "npm run build",
    "electron-pack-all": "build --win --macos -c.extraMetadata.main=build/main.js",
    "electron-pack": "build -c.extraMetadata.main=build/main.js"
  },
  "build": {
    "appId": "nonExistentID",
    "files": [
      "build/**/*",
      "node_modules/**/*"
    ],
    "directories": {
      "buildResources": "assets"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "electron": "^4.1.1",
    "electron-builder": "^20.39.0",
    "typescript": "^3.3.4000"
  }
}
